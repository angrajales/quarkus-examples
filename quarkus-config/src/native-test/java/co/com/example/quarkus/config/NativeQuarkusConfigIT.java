package co.com.example.quarkus.config;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeQuarkusConfigIT extends QuarkusConfigTest {

    // Execute the same tests but in native mode.
}